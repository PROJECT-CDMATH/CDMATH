CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

SET(COMMON_SUBDIRS INTERP_KERNEL MEDCoupling MEDCoupling_Swig HDF5 MEDFile MEDLoader)

SET(SUBDIRS ${COMMON_SUBDIRS})

FOREACH(DIR ${SUBDIRS})
  ADD_SUBDIRECTORY(${DIR})
ENDFOREACH(DIR ${SUBDIRS})
